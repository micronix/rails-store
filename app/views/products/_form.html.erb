<div class="form-group">
        <%= text_field_tag :name, @product.name, placeholder: 'Name', class: 'form-control' %>
    </div>
    <div class="form-group">
        <%= text_field_tag :description, @product.description, placeholder: 'Description', class: 'form-control' %>
    </div>
    <div class="form-group">
        <%= text_field_tag :picture_url, @product.picture_url, placeholder: 'Pricture URL', class: 'form-control' %>
    </div>
    <div class="form-group">
        <%= text_field_tag :price, @product.price, placeholder: 'Price', class: 'form-control' %>
    </div>
    <div class="form-group">
        <%= select_tag :category_id, options_from_collection_for_select(Category.all, "id", "name", @product.category_id), include_blank: true, class: 'form-control' %>
    </div>
    <%= submit_tag 'Save', class: 'btn btn-default' %>
